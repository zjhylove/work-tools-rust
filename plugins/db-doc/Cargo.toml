[package]
name = "db-doc"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# 插件 API
worktools-plugin-api = { path = "../../shared/plugin-api" }

# 序列化
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 错误处理
anyhow = "1.0"

# 数据库驱动 (编译时检查)
sqlx = { version = "0.8", features = ["mysql", "postgres", "runtime-tokio-rustls"] }

# 加密 (复用密码管理器方案)

# URL 编码
urlencoding = "2.1"

# 工具库
chrono = "0.4"
uuid = { version = "1.0", features = ["v4"] }
tokio = { version = "1.0", features = ["rt-multi-thread"] }
async-trait = "0.1"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
